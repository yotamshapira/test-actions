{
  "name": "test",
  "version": "1.0.0",
  "scripts": {
    "test": "JEST_PUPPETEER_CONFIG=test/jest-puppeteer.config.js jest -c test/jest.config.js --ci",
    "server": "http-server ."
  },
  "devDependencies": {
    "http-server": "^0.11.1",
    "jest": "^24.9.0",
    "jest-junit": "^8.0.0",
    "jest-puppeteer": "^4.3.0",
    "puppeteer": "^1.20.0"
  }
}
